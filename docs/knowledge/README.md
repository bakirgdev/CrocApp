# docs/knowledge

Evergreen project knowledge: what this project is, what croc is and how it works, domain facts that outlive individual features. Not a changelog, not task tracking (that's `TODO.md`), not decisions (those are `docs/decisions/`).

Written dense, for AI-session consumption first and humans second. Stale facts get fixed or deleted in the session that notices them (see root `CLAUDE.md`, "Docs self-heal").
