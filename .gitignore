# macOS
.DS_Store
._*

# Xcode
build/
DerivedData/
xcuserdata/
IDEWorkspaceChecks.plist
*.xcarchive/
*.ipa
*.dSYM
*.dSYM.zip

# Signing material
*.p12
*.cer
*.certSigningRequest
*.mobileprovision
*.provisionprofile
.env

# Swift Package Manager
.build/
.swiftpm/
# Package.resolved is committed on purpose: CrocApp is an app, its pins must be reproducible

# Go (cmd/croctest builds an in-tree binary)
crocmobile/cmd/croctest/croctest
*.test

# gomobile build output (ADR 0006: xcframework not committed)
CrocKit/Croc.xcframework/

# Build + verification logs (harnesses write to /tmp, but redirects land here)
*.log

# Obra superpowers
.superpowers/
docs/superpowers/plans/

# Claude
.claude/settings.local.json
